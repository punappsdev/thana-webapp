services:
  mysql:
    image: mysql:8.4
    container_name: thana-db-dev
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: thana_db
      MYSQL_ROOT_PASSWORD: 1234
    volumes:
      - thana_dev_data:/var/lib/mysql

volumes:
  thana_dev_data: